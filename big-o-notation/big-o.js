function doubleFor() {
    for (let i = 10; i < 10; i++) { // O(n)

    }
    for (let i = 10; i < 10; i++) { // O(n)

    }
}

// doubleFor(n + n) = O(n)

// examples
// O(n^2 + n) = O(n^2)
// O(n + log n) = O(n)
// O(5 * 2^n + 100000N) = O(2^n)
